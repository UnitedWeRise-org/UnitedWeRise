# Environment variables for admin/utility scripts
# Copy this file to .env in the backend directory (or set in your environment)
# DO NOT commit actual credentials to version control

# =============================================================================
# DATABASE CONNECTION STRINGS
# =============================================================================

# Production database (unitedwerise-db)
# Used by: delete-production-user.js, copy-complete-user.js, copy-full-account-prod-to-dev.js
PROD_DATABASE_URL="postgresql://username:password@unitedwerise-db.postgres.database.azure.com:5432/postgres?schema=public&sslmode=require"

# Development/Staging database (unitedwerise-db-dev)
# Used by: copy-complete-user.js, copy-full-account-prod-to-dev.js
DEV_DATABASE_URL="postgresql://username:password@unitedwerise-db-dev.postgres.database.azure.com:5432/postgres?schema=public&sslmode=require"

# Alternative: Some scripts also support PRODUCTION_DATABASE_URL or DATABASE_URL
# PRODUCTION_DATABASE_URL - Used by get-production-user.js as fallback
# DATABASE_URL - Default Prisma connection (from backend/.env)

# =============================================================================
# USAGE EXAMPLES
# =============================================================================

# Option 1: Set in your shell before running scripts
#   export PROD_DATABASE_URL="postgresql://..."
#   export DEV_DATABASE_URL="postgresql://..."
#   node scripts/copy-complete-user.js user@example.com

# Option 2: Add to backend/.env file (scripts use dotenv to load)
#   The scripts will automatically load from backend/.env

# Option 3: Use inline (not recommended - credentials in shell history)
#   PROD_DATABASE_URL="..." DEV_DATABASE_URL="..." node scripts/copy-complete-user.js user@example.com

# =============================================================================
# SECURITY NOTES
# =============================================================================

# - NEVER commit actual database credentials to version control
# - Use Azure Key Vault or similar secrets management in production
# - Rotate database passwords regularly
# - Use least-privilege database users when possible
# - These scripts require admin-level database access
