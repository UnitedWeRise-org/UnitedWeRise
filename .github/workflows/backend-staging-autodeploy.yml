name: Backend Auto-Deploy to Staging

on:
  push:
    branches:
      - development
    paths:
      - 'backend/**'
      - '.github/workflows/backend-staging-autodeploy.yml'

jobs:
  deploy-backend-staging:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    name: Deploy Backend to Staging Environment
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Build Docker Image in ACR
        env:
          AZURE_REGISTRY: uwracr2425
        run: |
          set -e

          echo "🚀 Starting backend deployment to STAGING"
          echo "Branch: development"
          echo "Commit: ${{ github.sha }}"
          echo "Timestamp: $(date)"

          # Get short commit SHA
          GIT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
          DOCKER_TAG="backend-dev-${GIT_SHA}-$(date +%Y%m%d-%H%M%S)"

          echo "🏷️ Docker tag: $DOCKER_TAG"

          # Build Docker image in ACR
          echo "🔨 Building Docker image in Azure Container Registry..."
          az acr build --registry $AZURE_REGISTRY \
            --image "unitedwerise-backend:$DOCKER_TAG" \
            --no-wait \
            https://github.com/${{ github.repository }}.git#development:backend

          echo "⏳ Waiting 180 seconds for build to complete..."
          sleep 180

          # Check build status
          echo "📊 Checking build status..."
          az acr task list-runs --registry $AZURE_REGISTRY --output table | head -3

          # Get image digest
          echo "🔍 Getting image digest..."
          DIGEST=$(az acr repository show --name $AZURE_REGISTRY \
            --image "unitedwerise-backend:$DOCKER_TAG" \
            --query "digest" -o tsv)

          echo "✅ Image digest: $DIGEST"

          # Save variables for next steps
          echo "GIT_SHA=$GIT_SHA" >> $GITHUB_ENV
          echo "DOCKER_TAG=$DOCKER_TAG" >> $GITHUB_ENV
          echo "DIGEST=$DIGEST" >> $GITHUB_ENV

      - name: Setup Node.js for Database Migrations
        uses: actions/setup-node@v6
        with:
          node-version: '20'

      - name: Install Prisma CLI
        run: |
          cd backend
          npm install prisma@latest --no-save

      - name: Run Database Migrations (Staging)
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL_STAGING }}
        run: |
          cd backend

          echo "================================================"
          echo "   DATABASE MIGRATION & VALIDATION (STAGING)"
          echo "================================================"
          echo ""

          echo "1️⃣ Validating schema.prisma syntax..."
          npx prisma validate --schema=prisma/schema.prisma
          echo "   ✅ Schema syntax is valid"
          echo ""

          echo "2️⃣ Checking migration status on STAGING database..."
          npx prisma migrate status || true
          echo ""

          echo "3️⃣ Detecting schema drift..."
          MIGRATE_STATUS=$(npx prisma migrate status 2>&1)
          echo "$MIGRATE_STATUS"

          if echo "$MIGRATE_STATUS" | grep -qi "drift"; then
            echo ""
            echo "⚠️  WARNING: Schema drift detected!"
            echo "   Database has been manually modified outside of Prisma migrations."
            echo "   This may cause deployment issues."
            echo ""
          fi

          echo ""
          echo "4️⃣ Applying pending migrations to STAGING..."
          npx prisma migrate deploy

          echo ""
          echo "5️⃣ Verifying migrations applied successfully..."
          npx prisma migrate status

          if npx prisma migrate status | grep -q "Database schema is up to date"; then
            echo ""
            echo "================================================"
            echo "   ✅ MIGRATIONS SUCCESSFUL (STAGING)"
            echo "   Database schema is consistent and up to date"
            echo "================================================"
          else
            echo ""
            echo "================================================"
            echo "   ❌ MIGRATION VERIFICATION FAILED"
            echo "   Database schema mismatch detected"
            echo "   Aborting deployment to prevent production issues"
            echo "================================================"
            exit 1
          fi

      - name: Deploy to Container App (Staging)
        timeout-minutes: 10
        env:
          AZURE_REGISTRY: uwracr2425
          AZURE_RESOURCE_GROUP: unitedwerise-rg
          AZURE_CONTAINER_APP: unitedwerise-backend-staging
        run: |
          # Deploy to Container App using saved environment variables
          echo "🚀 Deploying to Azure Container App (staging)..."
          echo "Image: ${AZURE_REGISTRY}.azurecr.io/unitedwerise-backend@${DIGEST}"

          az containerapp update \
            --name $AZURE_CONTAINER_APP \
            --resource-group $AZURE_RESOURCE_GROUP \
            --image "${AZURE_REGISTRY}.azurecr.io/unitedwerise-backend@${DIGEST}" \
            --revision-suffix "stg-${GIT_SHA}-$(date +%H%M%S)" \
            --set-env-vars \
              NODE_ENV=staging \
              STAGING_ENVIRONMENT=true \
              RELEASE_SHA=$GIT_SHA \
              RELEASE_DIGEST=$DIGEST \
            --no-wait

          echo "🔄 Setting revision mode to Single..."
          az containerapp update \
            --name $AZURE_CONTAINER_APP \
            --resource-group $AZURE_RESOURCE_GROUP \
            --revision-mode Single

          echo "⏳ Waiting for container to fully provision..."
          sleep 60

          echo "🔍 Polling for container readiness (max 2 minutes)..."
          for i in {1..12}; do
            HEALTH_RESPONSE=$(curl -s "https://dev-api.unitedwerise.org/health" || echo "")
            if [ -n "$HEALTH_RESPONSE" ] && echo "$HEALTH_RESPONSE" | grep -q "releaseSha"; then
              echo "✅ Container is ready!"
              break
            fi
            echo "Attempt $i/12: Container not ready yet, waiting 10 seconds..."
            sleep 10
          done

          # Verify deployment
          echo "✅ Verifying deployment..."
          HEALTH_RESPONSE=$(curl -s "https://dev-api.unitedwerise.org/health")
          echo "Health check response: $HEALTH_RESPONSE"

          # Check if deployed SHA matches
          DEPLOYED_SHA=$(echo $HEALTH_RESPONSE | grep -o '"releaseSha":"[^"]*"' | cut -d'"' -f4)
          echo "Expected SHA: $GIT_SHA"
          echo "Deployed SHA: $DEPLOYED_SHA"

          if [ "$DEPLOYED_SHA" = "$GIT_SHA" ]; then
            echo "✅ Deployment successful! Staging backend is running correct code."
          else
            echo "⚠️ Warning: Deployed SHA doesn't match expected SHA"
            echo "Deployment may still be starting up..."
          fi

          echo "🎉 Staging deployment complete!"
          echo "🔗 Backend: https://dev-api.unitedwerise.org"
          echo "🔗 Frontend: https://dev.unitedwerise.org"
